#include <stdio.h>
#include <stdlib.h>


int main(){
  FILE *d = fopen("r.txt","r");
  int n_timesteps = 10000 , n_particles = 256;
  double (*r) = malloc(sizeof(double [n_timesteps][n_particles]));

  for (int i ; i < N; ++i) {
    
  }


  




  return (0);
}
